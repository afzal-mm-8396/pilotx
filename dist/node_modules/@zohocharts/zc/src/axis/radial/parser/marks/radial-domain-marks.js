// import { mergeJSON } from "../../../../utils/zcutils";
import { ParserRegistry } from "../../../../core/parser-registry";
import { _cartesianDomain as domain } from "../../../cartesian/parser/marks/cartesian-domain-marks";

export function _radialDomain(data) {
    // reuse cartesian domain marks
    const self = this, KEY = 'axisline';

    const _position = this.helpers.position[KEY];

    const parser = domain.call(this, data);
    // const mark = {   //* use this for overriding
    //     properties: {
    //         attrs: {
    //             d: _position.end
    //         }
    //     }
    // };
    // mergeJSON(parser, mark, true, true);
    return parser;
}
ParserRegistry.axis('radial.domain', _radialDomain);
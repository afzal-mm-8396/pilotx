import { ParserRegistry } from "../../../../core/parser-registry";

export let ZC_RADIALAXIS_MARKS_MAPPING = function () {
    let mapping = {
        radial: {
            operator: ParserRegistry.axis('radial.default')
        }
    }
    return mapping;
}
import { defaultTheme as options } from '../core/defaulttheme';

options.heatmap = function() {
    return {
        strokeColor: 'white',
        animation: {},
        datalabels: {
            showAs: 'y'
        },
        gradients: {
            //linear | radial | none
            options: {
                linear: {
                    y2: 100
                },
                radial: {
                    radius: 100
                }
            }
        }
    };
};

export var _heatmapOptions = options.heatmap;

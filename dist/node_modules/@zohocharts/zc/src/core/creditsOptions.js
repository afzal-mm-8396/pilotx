import { defaultTheme as options } from '../core/defaulttheme';

options.credits = function(userProp) {
    return {
        enabled: false,
        imageWidth: 150,
        imageHeight: 50,
        hAlign: 'right', //left, center, "right"
        vAlign: 'bottom', //top, middle, bottom
        text: 'Zoho Reports',
        target: 'newtab', //newtab || newwindow || null
        marginTop: 0,
        marginRight: 10,
        marginBottom: 10,
        marginLeft: 0,
        fontSize: userProp.fontL1Size.l4
    };
};

export var _creditsOptions = options.credits;

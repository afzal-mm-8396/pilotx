//$Id$
//zc-constants.js
// zc constant functions and variables;

import { getDocumentWindow } from './main-zc-init';

export var UNDEFINED,
    math = Math,
    mathRound = math.round,
    mathFloor = math.floor,
    mathCeil = math.ceil,
    mathMax = math.max,
    mathMin = math.min,
    mathAbs = math.abs,
    mathPow = math.pow,
    mathSQRT = math.sqrt,
    mathCos = math.cos,
    mathSin = math.sin,
    mathLog = math.log,
    mathTan = math.tan,
    mathAtan2 = math.atan2,
    mathAtan = math.atan,
    mathLN10 = math.LN10,
    mathRandom = math.random,
    mathPI = math.PI,
    mathPI2 = mathPI * 2,
    deg2rad = (mathPI * 2) / 360,
    MAX_SAFE_INTEGER = Number.MAX_SAFE_INTEGER || 9007199254740991,
    EPSILON = Number.EPSILON || 2.220446049250313e-16,
    PX = 'px ',
    NONE = 'none',
    STROKE_WIDTH = 'stroke-width',
    TRANSPARENT = 'transparent',
    POINTER = 'pointer',
    NULL = null, //Below are not constants, need to change
    BASE_HELPER_CONTAINER = null,
    DOCUMENT_OBJECT_HOLDER = [],
    CURRENT_CHART_COUNT = 0,
    BASE_CONTAINER_POSITION = -9999,
    DEVICE_PIXEL_RATIO = getDocumentWindow().devicePixelRatio || 1,
    noOperation = (function() {
        var func = function() {};
        func._empty = true;
        return func;
    })();

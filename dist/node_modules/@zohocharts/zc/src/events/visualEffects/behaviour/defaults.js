import { hideCursor_C, showCursor_C } from "../controllers/cursor_c";
import { hidetooltip_c } from "../controllers/hidetooltip_c";
import { highlightTouch_c } from "../controllers/highlight-touch_c";
import { highlighter_c } from "../controllers/highlighter_c";
import { notes_c } from "../controllers/notes_c";
import { reset_c } from "../controllers/reset_c";
import { resethighlight_c } from "../controllers/resethighlight_c";
import { reversehighlight_c } from "../controllers/reversehighlight_c";
import { tooltip_c } from "../controllers/tooltip_c";
import { BehaviourMap } from "./behaviour-map";

const defaults = [
    {
        action: () => {
            return highlighter_c;
        },
        type: 'highlight'
    },
    {
        action: () => {
            return reversehighlight_c;
        },
        type: 'reverseHighlight'
    },
    {
        action: () => {
            return resethighlight_c;
        },
        type: 'resetHighlight'
    },
    {
        action: () => {
            return tooltip_c;
        },
        type: 'tooltip'
    },
    {
        action: () => {
            return hidetooltip_c;
        },
        type: 'hideTooltip'
    },
    {
        action: () => {
            return reset_c;
        },
        type: 'resetTooltip'
    },
    {
        action: () => {
            return showCursor_C;
        },
        type: 'cursor'
    },
    {
        action: () => {
            return hideCursor_C;
        },
        type: 'hideCursor'
    },
    {
        action: () => {
            return notes_c;
        },
        type: 'notes'
    },
    {
        action: () => {
            return highlightTouch_c;
        },
        type: 'highlightOnTaphold'
    }
];

// function conditionalDefault(args) {
//     return this.chart.isAxisCategory || this.chart.isPolarAxisCategory;
// }

defaults.forEach(behaviour => {
    BehaviourMap.set(behaviour);
});
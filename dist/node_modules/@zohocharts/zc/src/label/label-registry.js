import { Registry } from '../core/zc-registry';

export let LabelRegistry = {
    add: (name, component) => {
        Registry.setComponent(_key(name), component);
    },
    get: name => Registry.getComponent(_key(name))
};

function _key(name) {
    return `datalabels-${name}`;
}

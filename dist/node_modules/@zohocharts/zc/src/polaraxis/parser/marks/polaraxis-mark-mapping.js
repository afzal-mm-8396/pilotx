import { ParserRegistry } from "../../../core/parser-registry";

export let ZC_POLARAXIS_MARKS_MAPPING = function () {
    let mapping = {
        polar: {
            operator: ParserRegistry.axis('polar.default')
        }
    }
    return mapping;
}
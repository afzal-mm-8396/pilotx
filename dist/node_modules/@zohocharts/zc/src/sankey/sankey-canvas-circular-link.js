import { SVGParser } from '../utils/svg-parser';
import { sankeyCircularLink } from './sankey-circular-link';

export function sankeyCanvasCircularLink(context, link) {
    let pathString = sankeyCircularLink(link);
    SVGParser.drawPath(pathString, context);
}

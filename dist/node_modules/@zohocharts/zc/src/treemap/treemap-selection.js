import { barSelection } from '../bar/bar-selection';

export function TreemapSelection(points, mouse, chart, key) {
    if (!points) {
        return;
    }

    return barSelection(points, mouse, chart);
}

import { defaultTheme as options } from '../core/defaulttheme';

options.packedbubble = function() {
    return {
        fillOpacity: 0.2,
        outerPadding: 5,
        animation: {
            type: 'sizing' //sizing | fade | fadeAll
        },
        datalabels: {
            showAs: 'y' //x,y,percent //MODULARIZE
        }
    };
};

export var _packedbubbleOptions = options.packedbubble;

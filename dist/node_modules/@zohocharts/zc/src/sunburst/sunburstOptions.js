import { defaultTheme as options } from '../core/defaulttheme';
options.sunburst = function() {
    return {
        strokeColor: 'white',
        innerRadius: 30,
        outerPadding: 20,
        animation: {
            type: 'fan' //fan, inner, wheel
        },
        datalabels: {
            showAs: 'y', //x,y,percent
            type: 'circular', // circular || doubleside
            padding: 10,
            line: {
                strokeColor: 'grey',
                strokeWidth: 0.3
            },
            innerLabel: {
                show: false,
                showAs: 'splitpercent', //x,y,percent
                parentLabel: {}
            }
        },
        colorScheme: 'tints'    // tints, shades, solid
    };
};

export var _sunburstOptions = options.sunburst;

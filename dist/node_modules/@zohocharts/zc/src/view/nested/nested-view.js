//
import { BaseView } from '../base-view';
import { Registry } from '../../core/zc-registry';
import { NestedParser } from './nested-parser';

/*
spec = {
    bound: {
        width: 100,
        height: 100
    },
    data: [10, 20, 30],
    scale: d => d * 2,
    config: {
        shape: "circle",
        hAlign: "center",
        vAlign: "bottom",
        fillColor: "transparent",
        strokeColor: "#C0C2C4"
    }
}
 */

class NestedView extends BaseView {}
NestedView.parser = NestedParser;

export { NestedView };
Registry.setComponent('nestedView', NestedView);

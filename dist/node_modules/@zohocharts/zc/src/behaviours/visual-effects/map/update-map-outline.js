/**
 *
 * @param instanceMap
 * @param selectionManager
 * @param transform
 * @constructor
 */
export function UpdateMapOutLine(instanceMap, selectionManager, transform) {
    let mapEvents = this.chart.eventHandler.mapEvents;
    mapEvents.outLine = mapEvents.getOutLine();
}

export function PreventDefault(behaviourEventInstance, selectionManager, selection, event) {
    var sourceEvent = event.sourceEvent;
    if (sourceEvent && sourceEvent.preventDefault) {
        sourceEvent.preventDefault();
    }
}

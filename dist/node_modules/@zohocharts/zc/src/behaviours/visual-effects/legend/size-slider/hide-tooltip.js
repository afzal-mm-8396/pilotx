export function hideTooltip(behaviourEventInstance, selectionManager, selection, _, forced) {
    let brushInstance = behaviourEventInstance.instance,
        tooltip = brushInstance.tooltip;

    if (tooltip) {
        tooltip.hide(forced);
    }
}

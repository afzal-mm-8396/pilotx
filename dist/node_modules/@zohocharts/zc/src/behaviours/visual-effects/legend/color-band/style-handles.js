//
import { styleHandles, getRange } from '../../../../legend/colorband/colorband-brush-helpers';

function StyleHandles(behaviourEventInstance, selectionManager) {
    let _instance = behaviourEventInstance.instance,
        customHandles = _instance.customHandles,
        { brushOptions } = _instance.options.component,
        _selection = _instance.getSelection(),
        range = getRange(brushOptions, _selection);

    styleHandles(brushOptions, customHandles, range);
}

export { StyleHandles };

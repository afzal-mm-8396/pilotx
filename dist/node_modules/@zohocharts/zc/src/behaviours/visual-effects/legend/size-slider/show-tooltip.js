export function showTooltip(behaviourEventInstance, selectionManager, selection) {
    let brushInstance = behaviourEventInstance.instance,
        tooltip = brushInstance.tooltip;

    if (tooltip) {
        tooltip.positionTooltip();
        tooltip.show();
    }
}

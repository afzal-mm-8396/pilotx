/**
 *
 * @param instanceMap
 * @param event
 * @constructor
 */
export function MapZoomListener(instanceMap, event) {
    let eventManager = this;

    eventManager.associateBehaviours(instanceMap, event.type, {}, event.transform, event);
}

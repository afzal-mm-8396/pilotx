/**
 *
 * @param instanceMap
 * @param event
 * @constructor
 */
export function MapBrushListener(instanceMap, event) {
    let eventManager = this;

    if (event.selection) {
        eventManager.associateBehaviours(instanceMap, event.type, {}, event.selection, event);
    }
}

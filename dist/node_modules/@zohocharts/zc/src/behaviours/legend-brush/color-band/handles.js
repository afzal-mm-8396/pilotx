//
import { getBrushHandle } from '../../visual-effects/brush-handles/handles';

function getHandles(behaviour) {
    let behaviourInstance = behaviour.instance,
        { component, extent } = behaviourInstance.options,
        { brushOptions } = component,
        { type, config, handles: handleOptions } = brushOptions,
        { slider: sliderConfig } = config.filter;
    return getBrushHandle(type, sliderConfig.type, extent, null, sliderConfig[sliderConfig.type] || {});
}

export { getHandles };

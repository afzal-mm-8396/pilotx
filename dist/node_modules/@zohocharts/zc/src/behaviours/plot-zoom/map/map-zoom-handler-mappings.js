import { Registry } from '../../../core/zc-registry';
import { d3_set } from '../../../core/d3-constants';
import { MapZoomListener } from '../../listeners/map-zoom-listener';
import { ApplyZoomOnMap } from '../../visual-effects/map/apply-zoom-on-map';
import { UpdateMapOutLine } from '../../visual-effects/map/update-map-outline';
import { SaveGlobeMapState } from '../../visual-effects/map/globe-map-state';

let _mapZoomHandlers = {
    LISTENER: {
        start: MapZoomListener,
        zoom: MapZoomListener,
        end: MapZoomListener
    },
    DATA_FINDER: {},
    BEHAVIOUR: {
        start: SaveGlobeMapState,
        zoom: ApplyZoomOnMap,
        end: UpdateMapOutLine
    },
    LISTENER_TYPES: d3_set(['start', 'zoom', 'end']),
    SUPPORTED: chart => chart.isGeoMap
};

Registry.setComponent('_mapZoomHandlers', _mapZoomHandlers);

export { _mapZoomHandlers };

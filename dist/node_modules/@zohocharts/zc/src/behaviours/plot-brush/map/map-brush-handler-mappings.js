import { Registry } from '../../../core/zc-registry';
import { d3_set } from '../../../core/d3-constants';
import { MapBrushListener } from '../../listeners/map-brush-listener';
import { ApplyBrushOnMap } from '../../visual-effects/map/apply-brush-on-map';
import { SHOW_SELECTION_BOX } from '../../visual-effects/chart/controllers/brush-selection-box';

let _mapBrushHandlers = {
    LISTENER: {
        start: MapBrushListener,
        end: MapBrushListener
    },
    DATA_FINDER: {},
    BEHAVIOUR: {
        start: SHOW_SELECTION_BOX,
        end: ApplyBrushOnMap
    },
    LISTENER_TYPES: d3_set(['start', 'brush', 'end']),
    SUPPORTED: chart => chart.isGeoMap
};

Registry.setComponent('_mapBrushHandlers', _mapBrushHandlers);

export { _mapBrushHandlers };

import { Registry } from '../core/zc-registry';

export let ScaleRegistry = {
    add: (name, component) => {
        Registry.setComponent(name, component);
    },
    get: name => Registry.getComponent(name)
};


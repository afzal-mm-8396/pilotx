import { ScaleRegistry } from '../../scale-registry';

export function _pairs(values, first) {
    let pairs = [],
        previous = first;
    values.map(d => {
        pairs.push([previous, d[0]]);
        previous = d[0];
    });
    return pairs;
}

ScaleRegistry.add('color.utils._pairs', _pairs);

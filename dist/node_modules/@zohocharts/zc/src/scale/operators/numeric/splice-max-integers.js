import { MAX_SAFE_INTEGER } from '../../../core/zc-constants';

export function spliceMaxIntegers(array) {
    array.splice(0, 0, [-Infinity, -MAX_SAFE_INTEGER]);
    array.splice(array.length, 0, [MAX_SAFE_INTEGER, Infinity]);
    return array;
}

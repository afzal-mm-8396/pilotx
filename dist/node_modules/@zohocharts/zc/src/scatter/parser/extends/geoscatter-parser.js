import { ScatterRenderer } from "../scatter-parser";

export class GeoScatterRenderer extends ScatterRenderer {
    initialize(container) {
        super.initialize(container);
    }

    getAllowedMarkOperator() {
        return ['geoscatter'];
    }
}
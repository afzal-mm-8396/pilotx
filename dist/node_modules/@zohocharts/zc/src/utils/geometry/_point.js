import { mathSQRT, mathPow } from '../../core/zc-constants';

export function euclideanDistance(point1, point2) {
    let x = point2.x - point1.x,
        y = point2.y - point1.y;

    return mathSQRT(mathPow(x, 2) + mathPow(y, 2));
}

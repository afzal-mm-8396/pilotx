import { ArrayList } from '../utils/array-utils';

export function Leaf(name, depth, row, column) {
    (this.value = name), (this.depth = depth), (this.depthbyrow = row), (this.depthbycolumn = column), (this.datatype = 'numeric');
    this.data = [];
    this.index = 0;
    this.count = 0;
    this.name = 'Leaf';
}

Leaf.prototype = {
    constructor: Leaf
};

export function ResetHighlightItem() {
    let manager = this,
        { options } = manager,
        { highlightElement } = options;

    // Hide highlight element
    highlightElement.style('opacity', 0);
}

//
function UpdateLegendEvent() {
    let manager = this,
        { chart, options } = manager,
        { legendInstance } = options;

    chart.legendEvents.initiateBehaviour('SIZE_LIST', legendInstance);
    chart.legendEvents.initiateBehaviour('DISCRETE', legendInstance);
}

export { UpdateLegendEvent };

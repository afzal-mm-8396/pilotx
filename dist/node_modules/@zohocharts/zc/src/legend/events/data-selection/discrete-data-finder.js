export function DiscreteDataFinder(mouse, eventType, d, index, ele) {
    let manager = this,
        { options } = manager,
        { config, ticks } = options,
        { reversed } = config;

    return {
        data: d,
        i: index,
        ele: ele,
        type: 'legend'
    }
}


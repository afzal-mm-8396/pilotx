export function SizeListDataFinder(mouse, eventType, d, index) {
    let manager = this,
        { options } = manager,
        { config, ticks } = options,
        { reversed } = config;

    if (reversed) {
        return ticks.length - 1 - index;
    } else {
        return index;
    }
}

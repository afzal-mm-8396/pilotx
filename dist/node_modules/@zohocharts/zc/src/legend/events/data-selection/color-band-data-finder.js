//
import { getHighlightedItemFromMousePoint } from './size-slider-data-finder';

function ColorBandDataFinder(mouse, eventType) {
    let manager = this,
        { options } = manager;

    return getHighlightedItemFromMousePoint(mouse, options);
}

export { ColorBandDataFinder };

//
import { HORIZONTAL_ALIGN } from '../../core/align-constants';

function LegendTitleHtmlAdjustFactor(config, w, h, globalFont, align) {
    let isHorizontal = align.layout === HORIZONTAL_ALIGN,
        { show, keepAligned } = config,
        adjust = [0, 0];

    if (show && isHorizontal && keepAligned) {
        adjust[0] += w * 0.2;
    }

    return adjust;
}

export { LegendTitleHtmlAdjustFactor };

//
import { CENTER_ALIGN } from '../../../core/align-constants';
import { TRANSPARENT } from '../../../core/zc-constants';
import { Registry } from '../../../core/zc-registry';

export let shapeListOptions = userProp => {
    return {
        marker: {
            hAlign: CENTER_ALIGN,
            vAlign: CENTER_ALIGN,
            fillColor: TRANSPARENT,
            strokeColor: '#808080'
        },
        label: {
            fontSize: userProp.fontL1Size.l5,
            padding: 5,
            keepAligned: true
        }
    };
};

Registry.setComponent('shapeListOptions', shapeListOptions);

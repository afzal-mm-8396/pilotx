//
import { CENTER_ALIGN, BOTTOM_ALIGN, INNER_ALIGN } from '../../../core/align-constants';
import { TRANSPARENT } from '../../../core/zc-constants';
import { Registry } from '../../../core/zc-registry';

let sizeNestedOptions = userProp => {
    return {
        marker: {
            shape: 'circle',
            hAlign: CENTER_ALIGN,
            vAlign: BOTTOM_ALIGN,
            fillColor: TRANSPARENT,
            strokeColor: '#C0C2C4'
        },
        axis: {
            show: true,
            tickOrient: INNER_ALIGN,
            ticklabel: {
                fontSize: userProp.fontL1Size.l8
            },
            tickmark: {
                show: false,
                color: '#AAACAF',
                size: 10
            }
        }
    };
};

export { sizeNestedOptions };
Registry.setComponent('sizeNestedOptions', sizeNestedOptions);

import { BarRenderer } from '../bar/bar-parser';
// import { setGanttBuilderConfig } from './gantt-builder-config';
import { gantt_encode_collectors } from './gantt-encode-helpers';

export class GanttRenderer extends BarRenderer {
    collectEncode() {
        gantt_encode_collectors.call(this);
        // setGanttBuilderConfig.call(this);
    }
    getAllowedMarkOperator() {
        return ['connector', 'bar','levelmarker', 'marker'];
    }
}

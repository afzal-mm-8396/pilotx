import { defaultTheme as options } from '../core/defaulttheme';

options.dotgrid = function() {
    return {
        fillOpacity: 0.2,
        outerPadding: 0.2,
        animation: {
            type: 'grid'
        },
        datalabels: {
            showAs: 'x' //x,y,percent
        }
    };
};

export var _dotgridOptions = options.dotgrid;

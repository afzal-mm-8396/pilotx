import { defaultTheme as options } from '../core/defaulttheme';

options.wordcloud = function() {
    return {
        base: 0,
        strokeColor: 'transparent',
        size: 'auto',
        animation: {
            type: 'fadeOneByOne'
        },
        datalabels: {
            showAs: 'x' //x,y,percent
        },
        gradients: {
            options: {
                linear: {
                    x2: 100,
                    y2: 100,
                    colorGamma: [0, 1.5]
                },
                radial: {
                    x: 50,
                    y: 50,
                    radius: 100,
                    colorGamma: [1, 0]
                }
            }
        },
        mask: {
            background: {}
        }
    };
};

export var _wordcloudOptions = options.wordcloud;

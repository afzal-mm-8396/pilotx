//$Id$
import { Registry } from '../core/zc-registry';

export function Mark(mark, selector, data = [1], options) {
    return {
        mark,
        selector,
        data,
        children: [],
        ...options
    };
}

Registry.setComponent('mark', Mark);

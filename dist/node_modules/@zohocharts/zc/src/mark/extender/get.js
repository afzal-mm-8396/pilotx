import { isArray } from '../../utils/zcutils';
/*
 * n - property name to be collected fron selection
 * n can be passed in this format also - properties.styles.stroke, so we do split at first
 * selection.get - collects the passed property
 */


export function get(n) {
    var s = this.selection;
    var _g = [];
    var _n = isArray(n) ? n : [n];
    _n.map((d) => {
        var _d = d.split('.');
        if (isArray(s)) {
            for (var i = 0; i < s.length; i++) {
                _g.push(getPropVal(s[i], _d));
            }
        } else {
            _g.push(getPropVal(s, _d));
        }
    });
    return _g;
}   

/*
 * _n - property to be collected
 * _s - selection from which _n will be collected
 */
function getPropVal(_s, _n) {
    var i = 0;
    return _(_s);
    function _(_s) {
        if (i === _n.length - 1) {
            return _s[_n[i]];
        }
        return _(_s[_n[i++]]);
    }
}

import { isArray } from '../../utils/zcutils';
import { setProp } from './set';

/*
 * n - attribute name to be modified
 * v - attribute value to be assigned
 * s - selection
 * selection.config - modifies the given config with the given value
 */
export function config(n, v) {
    var s = this.selection;

    var _s = isArray(s) ? s : [s];
    for (var i = 0; i < _s.length; i++) {
        _.call(this, _s[i], n, v);
    }
    return this;
}
function _(_s, n, v) {
    var c = _s.config,
        _n = isArray(n) ? n : [n],
        _v = isArray(v) ? v : [v];

    c = _s.config = c ? c : {};
    _n.map((d, i) => {
        setProp.call(this, c, [d], _v[i]);
    });
}

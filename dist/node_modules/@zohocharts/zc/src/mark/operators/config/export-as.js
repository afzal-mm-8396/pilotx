//

import { findBestParser } from '../../helper/best-parser-finder';

function exportAs(_, applied) {
    let { name, as, onEnd = d => d } = _;

    let val = findBestParser(name, as, this)._as[as];

    return (d, i) => onEnd(val[i]);
}

export { exportAs };
